{
  "name": "pilot",
  "version": "0.0.0",
  "private": true,
  "description": "A terminal-first, provider-neutral coding-agent platform.",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "tsc -b --pretty false",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm versions:check",
    "clean": "tsc -b --clean",
    "eval": "vitest run --config vitest.evals.config.ts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "link:global": "pnpm build && pnpm --filter @pilotrun/cli exec npm link",
    "lint": "biome lint .",
    "package:smoke": "node scripts/package-smoke.mjs",
    "release:version": "node scripts/set-version.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --pretty false",
    "unlink:global": "pnpm --filter @pilotrun/cli exec npm unlink -g @pilotrun/cli",
    "versions:check": "node scripts/check-versions.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@types/node": "22.18.0",
    "node-pty": "catalog:",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
